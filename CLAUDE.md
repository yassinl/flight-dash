we're building a plane tracking app that runs on a raspberry pi

we don't have the matrix dashboard yet or the raspberry pi so we're building around it right now

the Z drive is the raspberry pi file system mounted on a windows host machine in powershell. there is an issue with write tool calls when working in this way. you should be able to use work arounds like bash scripts to still edit files